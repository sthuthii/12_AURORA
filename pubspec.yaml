name: ingredify
description: A new Flutter project

publish_to: 'none' # Remove this line if you want to publish to the Flutter package repository

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
